# 章尾結算獎勵圖（⑦）

- **占位**：`reward_placeholder.svg` — 開發用；正式版建議改為 **540×540 WebP**（與場景背景資產規範一致）。
- **建議檔名**：
  - 依洞察維度：`reward_ch{1-6}_{procedure|human|evidence}.webp`（對應 `data/chapterClosingRewards.ts` 的 `byDimension`）。
  - 第六章結局專用（選用）：在 `byEnding` 填入路徑，例如 `reward_ch6_ending_truth.webp`。
- 更新 `data/chapterClosingRewards.ts` 內路徑即可，無須改程式。
