<svg viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      .tag { fill: none; stroke: currentColor; stroke-width: 1.5; stroke-linecap: round; stroke-linejoin: round; }
      .label { fill: currentColor; opacity: 0.2; }
      .code { fill: currentColor; opacity: 0.6; }
    </style>
  </defs>
  <!-- d -->
  <rect class="tag" x="18" y="20" width="28" height="24" rx="2"/>
  <rect class="label" x="20" y="22" width="24" height="20" rx="1"/>
  <!-- ã¼ -->
  <text class="code" x="32" y="36" font-family="monospace" font-size="12" text-anchor="middle" fill="currentColor" opacity="0.8">C-17</text>
  <!-- T -->
  <circle class="tag" cx="32" cy="20" r="2" fill="currentColor" opacity="0.4"/>
</svg>
