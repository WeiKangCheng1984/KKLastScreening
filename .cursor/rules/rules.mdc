---
alwaysApply: true
---

# KK 流程偵探（現況版）專案規則

此文件是「現在程式碼實際運作」的準則，用來指引：資料怎麼放、UI 怎麼接、哪些地方不要再擴張，避免改一個點爆全章。

---

## 核心技術棧（仍有效）

- **Next.js 14（App Router）**：章節／場景以路由呈現（`/play/[chapterId]/[sceneId]`）
- **TypeScript**：以 `types/game.ts` 鎖住資料結構（Scene / Hotspot / Event / Requirement / Effect / Dialog / NpcDialogNode / Puzzle…）
- **Tailwind CSS**：暗色沉浸式 UI
- **Framer Motion + Lucide React**：過場與互動視覺

---

## 路由與頁面職責（現況）

| 路徑 | 說明 |
|---|---|
| `/` | 首頁 |
| `/play/prologue` | 序章 |
| `/play/[chapterId]/intro` | 章節介紹（CTA、導讀影片等） |
| `/play/[chapterId]/[sceneId]` | 主遊玩頁（場景 + Hotspot + 對話 + 謎題 + 章尾 overlay） |

主遊玩頁的核心整合檔仍是 `app/play/[chapterId]/[sceneId]/page.tsx`（高風險超大檔，見下方禁止事項）。

---

## 資料分章載入（現況真實流程）

### 章節定義
- **檔案**：`data/chapters.ts`
- **用途**：章節清單、intro meta、各章 scenes id 列表（routing/intro 依賴它）

### 分章資料（scenes/items/npcDialogs）
- **入口**：`data/getChapterData.ts`
- **回傳結構**：`{ scenes, items, npcDialogs }`
- **載入策略**：依 `chapterId` 動態 import `data/gameDataChX.ts`，縮小首包

### Play 頁如何載入
- **hook**：`hooks/useChapterData.ts`
- **流程**：
  1. `getChapterData(chapterId)` → `engine.loadChapterData(data)`
  2. `engine.applyEffect({ type: 'changeScene', chapterId, sceneId })`
  3. `ch2` 與其他章相同，僅注入 `scenes/items/npcDialogs`；章尾雙格題讀 `data/ch2ReportConfig.ts`

---

## GameEngine（現況）

- **檔案**：`lib/gameEngine.ts`
- **資料注入**：Engine 不直接 import 章節資料，透過 `loadChapterData()` 合併注入。
- **狀態**：`GameState` 含 `flags / inventory / interactions / score / weights / insights / preferences / reasoningAnswers / npcCasualTalkCount …`
- **強約束**：所有章節劇情/互動新增，優先寫進 `data/gameDataChX.ts`（scene.hotspotEventMap + events/effects 或 npcDialogs），不要把章節硬規則塞進 page.tsx。

---

## Hotspot 規格（現況）

### 座標系統
- **一律 0~1 比例**（禁止 px）
- **形狀**：
  - **主流**：`circle`，`coords: [cx, cy, r]`
  - `rect/poly` 僅特殊需求保留（新點優先用 circle）

### 點擊尺寸與縮放（現況正式值）
- **基準尺寸**：`clamp(40px, 4vw, 64px)` 乘上 `sizeFactor`
- **全域縮放**：由 `app/globals.css` 控制 `--hotspot-scale`
  - **手機**：0.77
  - **桌機（min-width: 768px）**：0.8
- **重要**：`components/SceneView.tsx` 用 inline `transform`（`translate(-50%, -50%) scale(var(--hotspot-scale)) translateZ(0)`），避免被 `.gpu-accelerated { transform: translateZ(0) }` 覆蓋。

### Debug 顯示
- `?debug=1`：顯示 hotspot 框與 id（正常模式透明可點）

---

## 對話系統（現況）

本專案目前同時存在三種「對話/敘事」來源，請依用途選對，不要混用造成重複旗標或流程卡死。

### 1) 一般對話（DialogBox queue）
- **來源**：事件 effect `showDialog` 或 page.tsx 內的 `addDialogsToQueue(...)`
- **用途**：旁白、系統訊息、道具描述、一般選擇題

### 2) NPC 對話樹（NpcDialogNode）
- **資料**：`data/gameDataChX.ts` 的 `npcDialogs`
- **結構**：`NpcDialogNode` 內含 `text / choices / next / effects`
- **用途**：敏感問題、分支追問、結尾設 flag（例如 `npc_*_sensitive_done`）

### 3) 角色多段台詞（已併入 DialogBox）
- **呈現**：play 頁一律 `DialogBox`（`variant="hotspot"`）+ `text`／`textSegments` 分段（與 `buildDialogFromNpcNode`、熱點 `showDialog` 相同管道）
- **立繪**：場景上 `components/NpcScenePortrait.tsx`；Dock 路徑 z-40 高於 `BottomDock` z-30；`HotspotZoomOverlay` 內立繪 z-20 高於 `DialogBox` `containedInOverlay` z-10

---

## 章尾結算／報告系統（現況重點）

### 第一章：向劉隊報告（時間線翻牌 + 5 題雙格填空）
- **入口**：`components/Ch1ReportEditor.tsx`
- **配置**：`data/ch1ReportConfig.ts`（`timeline`：`crimeTimeRange` + `errorMessages`；`attitude.attitudeFillBlanks` → `ch1ReportFillBlanks`；`closingInferenceByDimension`；`CH1_REPORT_DEFAULT_EVIDENCE_IDS` 僅供開 overlay 時寫入 `ch1_report_evidence` 旗標，無證據桌 UI）
- **題型元件**：`components/ReportFillBlank.tsx`
- **重要規則**：
  - 一次必須完成 **5 題**（index 0~4）
  - `ReportFillBlank` 必須帶 `key`（依題號），避免 React 沿用上一題內部 state 造成「只做一題就全過」。

### 第二章：向劉隊報告（5 題雙格填空）
- **元件**：`components/Ch2ReportEditor.tsx`（`ReportFillBlank`）
- **配置**：`data/ch2ReportConfig.ts` → `ch2ReportFillBlanks`（2 題）＋ `ch2PhoneRiddle`（手機省電謎）

---

## 章 2～6：向劉隊報告（共用模組）創意破關流程提案

> 目標：第二章到第六章的「報告/結算」沿用同一套章尾 overlay 模組（避免每章重新發明 UI），但在互動方式上做差異化：同樣是「向劉隊報告」，每章手感都不一樣、破關邏輯也更貼近該章主題。

### 共通規格（所有章節的章尾報告都遵守）

- **入口觸發**：
  - 玩家在章內達成「足夠進度」→ 劉隊頭像對話出現「要不要報告」選項（類似 `ch1_liu_report_now`）
  - 進度建議採 **里程碑旗標**：`chX_milestone_*`（至少 3 個），或「已完成敏感線索數 >= N」
- **章尾 overlay 行為**：
  - overlay 開啟期間：阻擋場景點擊（hotspotBlocked）
  - 完成後：設 `chX_reasoning_done=true`（或統一 `chapter_reasoning_done[chX]=true`），並導向下一章 intro
- **回饋原則（寫法一致）**：
  - 不用紅字說「錯」，而是「劉隊一句冷的」+「KK 旁白一句硬的」，提示玩家**缺了哪種支點**
  - 容許玩家反覆嘗試，但每次錯誤回饋要「有資訊」：缺時間？缺權限？缺現場痕跡？缺動機？
- **洞察/偏好加權（選配）**：
  - 完成時依玩家偏向（流程/證據/人心）給 `insights` 小幅加成，並用 closing text 收尾（類似 ch1 的三維度結語）

---

### 互動模組庫（同一套 UI 可重用的 4 種章尾玩法）

> 章 2～6 以「資料驅動」挑選其中 1～2 種組合。重點是：**同模組、換資料、換回饋語氣**。

#### 模組 A：五題（或三題）雙格浮動填空（延續 ch1）
- **形式**：每題 2 個空格、浮動選項卡；用 `components/ReportFillBlank.tsx`
- **適用**：需要「報告語感」的章（流程/權限/動線/人物動機）
- **過關**：每題兩格都對才算完成；允許多個正解（同義詞）
- **注意**：每題必須重置 state（`key` 依題號）

#### 模組 B：殘句推理（句子完成）+ 劉隊追問回應
- **形式**：每題選一段短語補句 → 立刻顯示劉隊追問/反問 → 再進下一題（章節可自訂節奏；ch2 已改為雙格填空）
- **適用**：證據量大、需要逐題拆解的章（比較「盤問」而非「填空」）
- **過關**：答完全部題目，或「正確題數 >= 門檻」（依章難度設定）

#### 模組 C：三封套分流（報告 / 私留 / 不寫）
- **形式**：拖曳卡片到三個容器（自建拖曳 UI；ch1 現況未使用此模組）
  - **警用報告封套**：能交出去、文字中性
  - **KK 私留備忘**：帶刺、可疑、還不能說死
  - **不寫**：太早、太薄、會害人（或會被拿去收工）
- **適用**：主題是「壓力、遮蔽、內部自保」的章
- **過關**：
  - 報告封套至少 2 張、私留至少 1 張
  - 必須把 1 張「關鍵卡」放入私留（避免玩家全上報）

#### 模組 D：動線板（路徑排序／排除）
- **形式**：玩家在 4～6 個「動線片段」中選出 3 段，組成「最可能路線」
  - UI 可做：先選 3 張再排序、或點選成串
- **適用**：主題是「監視器、死角、路徑、時間窗口」的章
- **過關**：必須覆蓋 3 類支點（入口/轉角/消失點）；少一類就回饋「你這條線會撞鏡頭/撞人潮」

---

## 章別破關流程（ch2～ch6）創意方向

> 不先綁死劇情細節，先定「報告的破關邏輯」：玩家到底要向劉隊交出哪一種結論。

### 第二章（ch2）：兩題雙格填空＋手機省電謎（現況）
- **實作**：`Ch2ReportEditor` + `data/ch2ReportConfig.ts`（`ReportFillBlank` 2 題 + `Ch2CrowPhoneRiddle`）
- **主題手感**：先補兩句能對卷的報告句；再於手機草稿用「省電顯影」讀出關鍵詞並輸入過關
- **旗標**：`ch2_q1_done`、`ch2_q2_done`、`ch2_phone_riddle_done`、`ch2_qa_reviewed_with_liu`、`ch2_reasoning_done`（舊存檔五題填空全滿仍視為 QA 完成，見 `flowController`）

### 第三章（ch3）：三題「反證」報告（把錯的說法排掉）
- **建議模組**：A（雙格填空）但縮成 3 題
- **題幹模板**（固定可複用）：
  - 「這不是 __1__，因為 __2__。」
  - 「要成立 __1__，必須先解釋 __2__。」
  - 「如果是 __1__，那 __2__ 不會這樣留下。」
- **過關判定**：至少 2 題雙格全對；第 3 題允許 1 格可接受（嚴肅但錯）→ 劉隊回饋「你方向有了，證據還不夠」

### 第四章（ch4）：三封套分流（哪些能上報、哪些要留底）
- **建議模組**：C（三封套）
- **主題手感**：你不是在寫作文，你是在決定「哪句話會害死人」
- **過關判定**：
  - 報告封套必須涵蓋「時間/權限/痕跡」至少各 1
  - 私留封套必須包含「壓力/恐懼/遮蔽」類卡（關鍵卡）

### 第五章（ch5）：動線板（逃逸路線重建）
- **建議模組**：D（動線板）
- **主題手感**：像在看監視器的「缺口」，把缺口拼成路線
- **過關判定**：必須選到「死角轉角」或「切出鏡頭的點」；否則劉隊回覆：「你這條線，鏡頭會拍到。」

### 第六章（ch6）：一頁報告（極簡三句，句句能活）
- **建議模組**：A（3 題雙格）或 B（3 題殘句），但壓縮成「三句定稿」
- **三句定位**：
  1) **可證明的事實**（證據）
  2) **可追的責任鏈**（流程/權限）
  3) **最危險的猜測**（人心/動機，但必須寫成「待查」）
- **過關判定**：第 3 句不要求絕對正確，但要求「寫得能交出去」：用 options 限制措辭，避免玩家寫死結論

---

## 落地寫法建議（讓它真能用同一套模組）

- **每章一份 report config**（像 `ch1ReportConfig` 一樣）：
  - `data/chXReportConfig.ts`：宣告本章使用哪個模組（A/B/C/D）+ 題目/卡片資料 + closing text
- **統一入口（現況已接線）**：
  - `data/getChapterConfig.ts` 的 `ChapterConfig.report`：`postComplete`（章尾完成後導向下一章 intro 的旗標與 href）。向劉隊回報以章尾 overlay 為主，已移除浮動 ReasoningPanel。
  - 章尾 overlay 元件集中於 `components/ChapterReportEditorHost.tsx`；劉隊點擊邏輯見 `lib/liuReportFlow.ts`，ch3～ch6 劉隊殘句 QA 見 `lib/liuQaDialogChoice.ts`。
- **對話呈現（可選）**：`types/game.ts` 的 `Effect.dialogPresentation` / `Scene.defaultDialogPresentation` / `Hotspot.hintPresentation`（`zoom` | `dock`）供資料層切換全螢幕層與 Dock。
- **旗標命名（建議）**：
  - `chX_liu_report_ready`：允許「向劉隊報告」選項出現
  - `chX_liu_report_done`：章尾結算完成

> 重點：章尾玩法做「同模組、不同資料」，避免把章特化邏輯再塞進 `page.tsx`。





---

## 道具規則（現況）

- **背包道具**：`collectible: true` 才會進 `inventory`
- **第一章道具精簡策略**：
  - 少量進背包（例如票根、黑色碎片）
  - 其餘採「檢視即發現」：事件只設 flag，不一定 addItem
  - 第一章章尾**無**證據桌互動；現場線索仍以場景事件旗標／背包為準（見 `gameDataCh1.ts`）

---

## 視覺／音效系統（現況）

- **光照**：`lib/lightingManager.ts` + `components/SceneView.tsx`（環境光會隨互動次數做緩慢調整）
- **音效策略**：`SceneView` 的 hotspot 點擊音效目前停用（保留 BGM 方向），不要在 hotspot click 補回大量 SFX，先確認效能與節奏。

---

## 開發與調試（現況）

- `?dev=1`：DeveloperPanel（旗標/跳轉/測試）
- `?debug=1`：顯示 hotspots（框 + id）

---

## 高風險檔案與協作規則（請嚴格遵守）

### 絕對禁止
1. **禁止在 React render 主體呼叫 engine 副作用**
   - 不可在 render（非 `useEffect/useCallback`）內做 `engine.applyEffect/triggerEvent/...`
2. **禁止繼續膨脹 `page.tsx` 的章節特化 if-else**
   - 新互動請走資料驅動：`scene.hotspotEventMap` + `events.effects` 或 `npcDialogs`
3. **避免新增 `setRefreshKey(prev => prev + 1)`**
   - 這是 force update 反模式；若真需要，先找出缺少的 state 派生/依賴，或用 ref 鏡像解 stale closure

### 強建議
- **大型 handler 提取**：不要在 JSX 直接塞超長匿名函式（可讀性與效能都很差）
- **setTimeout 要可清理**：新增計時器優先放 `useEffect` 並 cleanup
- **isMobileDevice 統一**：請使用 `lib/performanceUtils.ts` 的版本（避免寬度判斷與 UA 判斷不一致）

---

## 命名與資產規範（仍適用）

### 場景背景
- 540×540、WebP、≤250KB
- `bg_ch{章}_sc{場}_v{版}.webp`

### 道具圖
- 長邊 384（必要才 512）、WebP/PNG、≤80KB（特寫可到 120KB）
- `item_{id}_v{版}.webp`

### 角色頭像
- WEBP 優先：`public/images/characters/{characterId}_{1|2|3}.webp`
- SVG 備援：`/svg/characters/{characterId}.svg`
