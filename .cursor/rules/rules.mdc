---
alwaysApply: true
---

# 核心框架

Next.js 14（App Router）：章節 / 場景用路由呈現（/play/[chapterId]/[sceneId]）
TypeScript 5：用型別鎖住內容結構（Scene/Hotspot/Event/Requirement/Effect），避免劇情越改越崩
Tailwind CSS 3：快速做沉浸式 UI（暗色、霧面、卡片、對話框）
Lucide React：線索牆、音量、返回、提示等輕量圖示
Framer Motion：動畫效果（淡入淡出、粒子效果等）
部署：Vercel：靜態資產 + SSR/CSR 混用、快速迭代上線

---

## 【1】場景背景圖（BG）

- **固定比例**：1:1（正方形）
- **固定尺寸**：540×540px
- **格式**：PNG
- **單張大小上限**：≤ 250KB（超過就重壓或降畫質）
- **命名**：`bg_ch{章}_sc{場}_v{版}.png`
- **範例**：`bg_ch1_sc1_v1.png`（ROOM 1, SPACE 1, 版本1）

---

## 【2】物件圖（道具/線索/特寫）

- **尺寸**：長邊 384px（需要更清楚才用 512px）
- **格式**：
  - 需要透明：PNG
  - 一般物件：WebP（較小）
- **單張大小上限**：≤ 80KB（特寫可放寬到 120KB）
- **命名**：`item_{id}_v{版}.webp/png`
- **範例**：`item_faded_photo_v1.png`

---

## 【3】UI 圖

- **圖示**：一律用 Lucide React（SVG），不做 PNG icon
- **尺寸**：540×540 或更小
- **大小**：≤ 80KB

---

## 【4】Hotspot（透明點選區）

- **座標系統**：一律用比例值（0~1），不要用 px
- **形狀**：先只用矩形 `rect`（真的不規則才用 `poly`）
- **最小可點尺寸**：手機至少等效 44×44px
- **響應式尺寸**：使用 `clamp(40px, 4vw, 64px)` 確保跨裝置適配
- **視覺樣式**：白色半透明邊框（`border-white/30`），背景（`bg-white/5`）
- **提醒效果**：若隱若現的脈衝動畫（`animate-ping`, `animate-pulse`），使用白色低透明度
- **預設禁止重疊**（不要做 priority 複雜規則）
- **必做 debug**：`?debug=1` 顯示 hotspot 框與 id（正常模式透明可點）

---

## 【5】一致性規則（避免改圖就崩）

- **背景改版**：只允許調色/光影/霧感，不允許移動可互動物件位置
- **移動物件**：必須同步更新 hotspots（當作新版本處理）

---

## 【6】遊戲引擎系統（GameEngine）

### 核心功能
- **狀態管理**：`GameState` 包含當前章節、場景、道具、flags、互動記錄、分數等
- **狀態持久化**：使用 `localStorage` 自動保存遊戲狀態
- **事件系統**：基於 `Requirement` 條件觸發 `Event`，執行 `Effect`
- **謎題系統**：支援 15+ 種謎題類型，基於條件解鎖
- **分數系統**：追蹤玩家選擇的權重（`weight`），計算總分數和各維度權重

### 主要方法
- `checkRequirement(req)`：檢查單個需求條件
- `checkEventRequirements(event)`：檢查事件所有需求
- `triggerEvent(eventId)`：觸發事件並應用效果
- `applyEffect(effect)`：應用效果（添加道具、設置 flag、顯示對話等）
- `solvePuzzle(puzzleId, solution)`：解決謎題
- `addChoice(choiceId, weight)`：記錄玩家選擇並更新分數

---

## 【7】對話系統

### 雙系統架構

#### 7.1 普通對話系統（DialogBox）
- **用途**：一般對話、道具描述、系統訊息、旁白
- **類型**：`narrator` | `broadcast` | `item` | `system` | `choice` | `character`
- **功能**：
  - 打字機效果（可配置速度）
  - 對話歷史記錄
  - 選擇題支援（2選1或3選1）
  - SVG 圖片顯示
- **觸發方式**：通過 `Event` 的 `showDialog` 效果

#### 7.2 角色對話系統（CharacterConversation）
- **用途**：角色互動對話，形成有來有往的對話流程
- **特點**：
  - 自動連續播放多段對話
  - 支持角色立繪顯示
  - 區分玩家和角色對話（`speaker: 'player' | 'character'`）
  - 打字機效果 + 淡入淡出動畫（無位置跳動）
  - 每段對話完成後顯示「繼續」按鈕（玩家無法跳過）
  - 最後一段可顯示選擇題
- **配置位置**：`data/characterConversations.ts`
- **觸發方式**：點擊角色 HOTSPOT 時自動識別並使用此系統

---

## 【8】謎題系統

### 支援的謎題類型（15+ 種）

1. `input` - 文字輸入謎題
2. `sequence` - 順序謎題
3. `arrangement` - 排列謎題
4. `combination` - 組合謎題
5. `visual_selection` - 視覺選擇謎題
6. `combination_lock` - 密碼鎖
7. `word_scramble` - 拼字遊戲
8. `wire_connection` - 顏色線對接
9. `jigsaw` - 拼圖
10. `rotating_dial` - 旋轉轉盤
11. `sequence_memory` - 序列記憶
12. `sliding_puzzle` - 滑塊拼圖
13. `symbol_matching` - 符號配對
14. `maze_path` - 迷宮路徑
15. `logic_switches` - 邏輯開關

### 謎題配置
- **解鎖條件**：通過 `requirements` 定義
- **解決後效果**：通過 `onSolve` 定義（可觸發事件、添加道具等）
- **章節謎題**：每個章節可有一個章節謎題，達到探索進度閾值後解鎖

---

## 【9】道具系統

### 道具類型
- **可收集**：`collectible: true`，可加入背包
- **可使用**：`usable: true`，可在特定場景使用
- **顯示方式**：支持 PNG/WebP 圖片或 SVG

### 道具互動
- **收集**：點擊 HOTSPOT → 檢查條件 → 添加道具 → 顯示對話
- **查看**：點擊背包中的道具 → 顯示描述對話
- **使用**：在特定場景使用道具觸發事件

---

## 【10】事件系統

### 事件觸發流程
1. 玩家點擊 HOTSPOT
2. 系統查找 `hotspotEventMap` 對應的事件 ID
3. 檢查事件 `requirements`（道具、flag、互動記錄等）
4. 條件滿足 → 觸發事件 → 應用 `effects`
5. 如果是 `oneTime: true`，標記為已互動

### 事件效果類型
- `addItem` - 添加道具
- `removeItem` - 移除道具
- `setFlag` - 設置 flag
- `showDialog` - 顯示對話
- `playAudio` - 播放音效
- `triggerEvent` - 觸發其他事件
- `changeScene` - 切換場景

### 特殊事件：角色對話
- 事件 ID 匹配模式：`talk_to_character_*` 或 `character_*_talk`
- 自動使用 `CharacterConversation` 系統
- 從 `characterConversations.ts` 載入對話配置

---

## 【11】分數與選擇系統

### 分數追蹤
- **總分數**：`state.score`（累加所有選擇的權重）
- **維度權重**：`state.weights`（記錄各維度的權重，如 `acceptance`, `resistance`, `hesitation`）
- **選擇記錄**：`state.choices`（記錄所有選擇的時間戳和權重）

### 選擇權重
- **正權重**：接受、承擔責任等（如 `weight: 10`）
- **負權重**：拒絕、離開等（如 `weight: -5`）
- **零權重**：中性選擇

### 顯示組件
- `ScoreDisplay`：顯示總分數、各維度權重、選擇次數
- 整合在右上角選單中

---

## 【12】音效系統（AudioManager）

### 音效類型
- **環境音**（Ambient）：場景背景音效
- **廣播音效**（Broadcast）：廣播靜電音等
- **恐怖音效**（Horror）：呼吸聲、心跳聲、風聲等
- **音效**（SFX）：門解鎖、金屬聲、紙張翻動等

### 管理功能
- 自動播放/停止環境音
- 音效音量控制
- 背景音樂管理

---

## 【13】UI 系統

### 主要 UI 組件
- **選單系統**：右上角統一選單（退出、音效、背包、開發者模式、分數顯示）
- **背包系統**：浮動覆蓋層，不壓縮主畫面
- **對話框**：固定尺寸 360×360px，支持打字機效果
- **開發者面板**：`?dev=1` 啟用，顯示狀態、快速跳轉等
- **章節介紹**：支持文字、圖片、影片、輪播
- **章節影片**：支持播放、重播、暫停、跳過

### 響應式設計
- 使用 `clamp()` 函數實現響應式尺寸
- 手機端和桌面端統一 UI
- HOTSPOT 使用響應式尺寸（`clamp(40px, 4vw, 64px)`）

---

## 【14】視覺效果系統

### 場景效果
- **閃爍效果**：已移除（之前用於恐怖氛圍）
- **環境光調整**：根據場景調整亮度
- **過渡動畫**：場景切換時的淡入淡出

### 動畫效果
- **Framer Motion**：用於對話切換、組件進入/退出
- **淡入淡出**：角色對話使用 `opacity` 動畫（無位置跳動）
- **打字機效果**：對話文字逐字顯示

---

## 【15】章節與場景系統

### 章節結構
- **5 個 ROOM**，每個 ROOM 包含 3 個 SPACE（場景）
- **章節介紹**：標題、副標題、描述、心理文字、背景圖/影片、導讀影片
- **章節謎題**：可選，達到探索進度閾值後解鎖

### 場景切換
- **左右箭頭**：切換同章節內的場景
- **場景選擇器**：快速跳轉到已訪問的場景
- **自動保存**：場景切換時自動保存狀態

---

## 【16】開發與調試

### 開發者模式
- **啟用方式**：URL 參數 `?dev=1`
- **功能**：
  - 開發者面板（狀態查看、快速跳轉）
  - 調試日誌
  - 場景選擇器
  - Flag 管理

### Debug 模式
- **啟用方式**：URL 參數 `?debug=1`
- **功能**：顯示所有 HOTSPOT 的邊框和 ID

---

## 【17】資料結構

### 主要資料檔案
- `data/gameData.ts`：場景、道具、事件、謎題、章節定義
- `data/characterConversations.ts`：角色對話鏈配置
- `types/game.ts`：所有 TypeScript 型別定義

### 狀態持久化
- **localStorage**：自動保存 `GameState`
- **恢復機制**：頁面重新載入時自動恢復狀態

---

## 【18】檔案命名規範

### 場景背景圖
- `bg_ch{章}_sc{場}_v{版}.png`

### 道具圖
- `item_{id}_v{版}.webp/png`

### 角色立繪
- `/svg/characters/{characterId}.svg`

### 音效檔案
- 環境音：`/audio/ambient/{name}.mp3`
- 廣播：`/audio/broadcast/{name}.mp3`
- 恐怖音效：`/audio/horror/{name}.mp3`
- 音效：`/audio/sfx/{name}.mp3`

### 章節影片
- 導讀影片：`/videos/intro_ch{章}_animation_v{版}.mp4` 或自訂檔名（如 `木木.mp4`）

---

## 【19】重要設計原則

1. **型別安全**：所有遊戲內容都通過 TypeScript 型別定義，避免結構錯誤
2. **條件驅動**：事件和謎題都基於條件系統，確保邏輯清晰
3. **狀態管理**：所有遊戲狀態由 `GameEngine` 統一管理
4. **組件化**：UI 組件獨立，易於維護和擴展
5. **響應式優先**：所有 UI 元素都考慮手機和桌面端適配
6. **無跳過機制**：角色對話必須完整觀看，確保劇情體驗
7. **視覺穩定**：使用淡入淡出動畫，避免位置跳動造成視覺疲勞
