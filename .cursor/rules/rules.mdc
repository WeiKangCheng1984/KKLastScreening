---
alwaysApply: true
---

# 核心框架

- **Next.js 14**（App Router）：章節／場景以路由呈現（`/play/[chapterId]/[sceneId]`）
- **TypeScript 5**：型別鎖住內容結構（Scene / Hotspot / Event / Requirement / Effect / Dialog / ConversationTurn），避免劇情越改越崩
- **Tailwind CSS 3**：沉浸式 UI（暗色、霧面、卡片、對話框）
- **Lucide React**：線索牆、音量、返回、提示、X、ChevronRight、User 等輕量圖示
- **Framer Motion**：動畫效果（淡入淡出、粒子、AnimatePresence）
- **部署**：Vercel，靜態資產 + SSR/CSR 混用、快速迭代上線

---

# 技術架構與專案結構

## 路由一覽（App Router）

| 路徑 | 說明 |
|------|------|
| `/` | 首頁 |
| `/play` | 遊戲入口（需導向章節／場景） |
| `/play/prologue` | 序章 |
| `/play/animation` | 動畫頁 |
| `/play/[chapterId]/intro` | 章節介紹（標題、導讀影片等） |
| `/play/[chapterId]/[sceneId]` | 主遊玩頁：場景探索、Hotspot、對話、謎題 |
| `/play/ch2/hub` | 第二章 Hub（若存在） |

## 目錄與關鍵檔案

| 目錄／檔案 | 用途 |
|------------|------|
| `app/` | 頁面與 layout（`layout.tsx`, `page.tsx`, `globals.css`） |
| `app/play/[chapterId]/[sceneId]/page.tsx` | 場景主頁：SceneView、GameEngine、DialogBox、CharacterConversation、Hotspot、背包、道具取得、NPC 對話 |
| `components/` | 所有 UI 組件（見下方組件清單） |
| `lib/` | 遊戲邏輯與工具：`gameEngine.ts`、`characterPortrait.ts`、`audioManager.ts`、`flowController.ts`、`chapterPassword.ts`、`responsiveUtils.ts`、`svgLoader.ts`、`svgPreloader.ts`、`svgDragSystem.ts`、`performanceUtils.ts`、`transitionEffects.ts`、`lightingManager.ts` |
| `data/` | `gameDataCh1.ts`～`gameDataCh6.ts`（各章場景／道具／事件／npcDialogs）、`getChapterData.ts`、`chapters.ts`、`characterConversations.ts`、`reasoningByChapter.ts`、`flowConfig.ts`；另有 `flagTestConfig.ts`、`ch1PasswordPresets.ts`、`ch1Monologue.ts`、`ch1ReportConfig.ts` 等章節或流程用設定 |
| `types/game.ts` | 所有 TypeScript 型別（GameState, Scene, Event, Dialog, ConversationTurn, DialogChoice, Item, Puzzle, Npc, InsightsState 等） |
| `public/` | 靜態資產：背景圖、道具圖、`/images/characters/` 角色頭像 WEBP、`/audio/`、`/videos/`、`/svg/` |

## 關鍵組件與檔案對照

| 功能 | 組件／檔案 |
|------|------------|
| 場景主畫面、Hotspot、背景 | `SceneView.tsx` |
| 一般對話、道具描述、系統訊息 | `DialogBox.tsx` |
| 角色多段對話（NPC 對話框） | `CharacterConversation.tsx`（邏輯）+ `CharacterConversationCard.tsx`（UI，含繼續／X／選擇題） |
| 取得道具浮動提示 | `ItemObtainedNotification.tsx` |
| 背包 | `Inventory.tsx`、`ItemIcon.tsx` |
| 場景內 NPC 頭像條 | `NpcRightStrip.tsx` |
| 章節介紹、導讀影片 | `ChapterIntro.tsx`（分層顯示 200/800/1600/2600ms，CTA 按鈕「開始調查」或「觀看影片」） |
| 謎題 | `ChapterPuzzle.tsx`、各謎題組件（PuzzleInput, CombinationLock, JigsawPuzzle, SlidingPuzzle, WordScramble, WireConnection, RotatingDial, SymbolMatching, MazePath, SequenceMemory, LogicSwitches, VisualSelectionPuzzle, ArrangementPuzzle, PairMatchingPuzzle, PickThreePuzzle 等） |
| 分數與選擇 | `ScoreDisplay.tsx` |
| 開發者／Debug | `DeveloperPanel.tsx`（`?dev=1`）、場景 debug（`?debug=1` 顯示 Hotspot） |
| 其他 UI／效果 | `SceneNameDisplay`、`SceneTransition`、`AudioControl`、`DialogChoice`、`effects/`、`animations/`、`puzzle-svg/` |

---

# 玩法概要

- **探索**：在章節內切換場景（左右箭頭、場景選擇器），點擊 Hotspot 觸發事件。
- **收集**：符合條件時取得道具，僅顯示浮動提示（ItemObtainedNotification），不排入一般對話佇列；背包點擊道具與「首次發現」同呈現（浮動提示＋描述）。
- **對話**：一般對話（DialogBox）與角色多段對話（CharacterConversation）雙系統；角色對話可帶選擇題、影響分數／洞察。
- **謎題**：章節謎題與場景內謎題，依條件解鎖，解完可觸發事件／道具。
- **分數與選擇**：選擇權重寫入 `state.score`、`state.weights`、`state.choices`；洞察三維度（procedure_insight, human_insight, evidence_insight）與偏好變量由 `types/game.ts` 定義。

---

## 【1】場景背景圖（BG）

- **固定比例**：1:1（正方形）
- **固定尺寸**：540×540px
- **格式**：WebP（優先；需透明或相容性時可用 PNG）
- **單張大小上限**：≤ 250KB（超過就重壓或降畫質）
- **命名**：`bg_ch{章}_sc{場}_v{版}.webp`
- **範例**：`bg_ch1_sc1_v1.webp`（ROOM 1, SPACE 1, 版本1）

---

## 【2】物件圖（道具/線索/特寫）

- **尺寸**：長邊 384px（需要更清楚才用 512px）
- **格式**：需要透明用 PNG，一般用 WebP
- **單張大小上限**：≤ 80KB（特寫可放寬到 120KB）
- **命名**：`item_{id}_v{版}.webp`（優先）或 `.png`
- **範例**：`item_faded_photo_v1.webp`

---

## 【3】UI 圖

- **圖示**：一律用 Lucide React（SVG），不做 PNG icon
- **尺寸**：540×540 或更小，大小 ≤ 80KB

---

## 【4】Hotspot（透明點選區）

- **座標系統**：一律用比例值（0~1），不要用 px
- **形狀**：先只用矩形 `rect`（真的不規則才用 `poly`）
- **最小可點尺寸**：手機至少等效 44×44px
- **響應式尺寸**：使用 `clamp(40px, 4vw, 64px)` 確保跨裝置適配
- **視覺樣式**：白色半透明邊框（`border-white/30`），背景（`bg-white/5`）
- **提醒效果**：若隱若現的脈衝動畫（`animate-ping`, `animate-pulse`），白色低透明度
- **預設禁止重疊**（不要做 priority 複雜規則）
- **必做 debug**：`?debug=1` 顯示 hotspot 框與 id（正常模式透明可點）

---

## 【5】一致性規則（避免改圖就崩）

- **背景改版**：只允許調色/光影/霧感，不允許移動可互動物件位置
- **移動物件**：必須同步更新 hotspots（當作新版本處理）

---

## 【6】遊戲引擎系統（GameEngine）

### 核心功能
- **狀態管理**：`GameState` 包含當前章節、場景、道具、flags、互動記錄、分數、weights、choices、preferences、insights 等
- **狀態持久化**：使用 `localStorage` 自動保存遊戲狀態
- **事件系統**：基於 `Requirement` 條件觸發 `Event`，執行 `Effect`
- **謎題系統**：支援 15+ 種謎題類型，基於條件解鎖
- **分數系統**：追蹤玩家選擇的權重（`weight`），計算總分數與各維度權重

### 主要方法
- `checkRequirement(req)`：檢查單個需求條件
- `checkEventRequirements(event)`：檢查事件所有需求
- `triggerEvent(eventId)`：觸發事件並應用效果
- `applyEffect(effect)`：應用效果（添加道具、設置 flag、顯示對話等）
- `solvePuzzle(puzzleId, solution)`：解決謎題
- `addChoice(choiceId, weight)`：記錄玩家選擇並更新分數

### 實作位置
- **檔案**：`lib/gameEngine.ts`
- **資料**：不直接 import gameData；由 play 頁呼叫 `getChapterData(chapterId)` 取得該章 `scenes`、`items`、`npcDialogs` 後呼叫 `engine.loadChapterData(data)` 合併（ch1～ch6 對應 `data/gameDataCh1.ts`～`gameDataCh6.ts`）。

---

## 【7】對話系統

### 雙系統架構

#### 7.1 普通對話系統（DialogBox）
- **用途**：一般對話、道具描述、系統訊息、旁白
- **類型**：`narrator` | `broadcast` | `item` | `system` | `choice` | `character`
- **功能**：打字機效果（可配置速度）、對話歷史、選擇題（2選1或3選1）、SVG 圖片、**標題**（`Dialog.title`）
- **觸發方式**：通過 `Event` 的 `showDialog` 效果；或由 `addDialogsToQueue(interactionName)` 等排隊顯示
- **立繪不跳動**：同角色連續兩則時，`handleDialogClose` 不先 `setCurrentDialog(null)`，改為直接 `setCurrentDialog(nextDialog)` 並 `setDialogQueue(prev => prev.slice(1))`；立繪區塊使用 `effectiveDialog = currentDialog ?? dialogQueue[0]` 作為顯示來源，過渡期立繪不卸載。

#### 7.2 角色對話系統（CharacterConversation + CharacterConversationCard）
- **用途**：角色互動對話，有來有往的多段對話流程
- **實作分工**：
  - **CharacterConversation.tsx**：狀態與邏輯（currentIndex、打字機、handleNext、handleChoice），**不使用 JSX**，以 `React.createElement(CharacterConversationCard, props)` 渲染，避免編譯器將 `return (` 後的 `<` 誤判為比較運算符。
  - **CharacterConversationCard.tsx**：純 UI，接收 props，含人像、對話氣泡、**右上角操作列**、下方選擇題區。
- **特點**：
  - 自動連續播放多段對話（`ConversationTurn[]`）
  - 角色立繪：優先 `lib/characterPortrait.ts` 的 WEBP（`characterId` + `characterExpression`），其次 `characterPortrait`（SVG）
  - 區分玩家與角色（`speaker: 'player' | 'character'`）
  - 打字機效果 + 淡入淡出動畫（無位置跳動）
  - **繼續按鈕**：在**對話框右上角**，位於 **X 關閉鈕左邊**；點 X 呼叫 `onClose`（通常為 `onComplete`）
  - 最後一段可顯示選擇題（在下方固定高度按鈕區，選項多時區塊內捲動）
- **配置位置**：`data/characterConversations.ts`
- **觸發方式**：點擊角色 HOTSPOT，事件 ID 匹配 `talk_to_character_*` 或 `character_*_talk`，自動載入對應對話鏈

---

## 【8】謎題系統

### 支援的謎題類型（15+ 種）
1. `input` - 文字輸入謎題（PuzzleInput）
2. `sequence` - 順序謎題
3. `arrangement` - 排列謎題（ArrangementPuzzle）
4. `combination` - 組合謎題
5. `visual_selection` - 視覺選擇（VisualSelectionPuzzle）
6. `combination_lock` - 密碼鎖（CombinationLock）
7. `word_scramble` - 拼字（WordScramble）
8. `wire_connection` - 顏色線對接（WireConnection）
9. `jigsaw` - 拼圖（JigsawPuzzle）
10. `rotating_dial` - 旋轉轉盤（RotatingDial）
11. `sequence_memory` - 序列記憶（SequenceMemory）
12. `sliding_puzzle` - 滑塊拼圖（SlidingPuzzle）
13. `symbol_matching` - 符號配對（SymbolMatching）
14. `maze_path` - 迷宮路徑（MazePath）
15. `logic_switches` - 邏輯開關（LogicSwitches）

### 謎題配置
- **解鎖條件**：透過 `requirements` 定義
- **解決後效果**：透過 `onSolve` 定義（可觸發事件、添加道具等）
- **章節謎題**：每章可有一個章節謎題，達到探索進度閾值後解鎖（ChapterPuzzle）

---

## 【9】道具系統

### 道具類型
- **可收集**：`collectible: true`，可加入背包
- **可使用**：`usable: true`，可在特定場景使用
- **顯示方式**：支援 PNG/WebP 圖片或 SVG（`item.image`、`item.svgImage`）

### 道具互動
- **收集**：點擊 HOTSPOT → 檢查條件 → 添加道具 → **僅顯示浮動提示**（ItemObtainedNotification），不將「獲得：xxx」排入一般對話佇列
- **取得道具浮動提示**（ItemObtainedNotification）：
  - **方案 C**：`dismissOnTap: true` 時不自動關閉，顯示道具名稱與**描述**，點擊卡片或 X 關閉；關閉後呼叫 `onComplete`
  - 傳入 `itemDescription`、`itemImage`/`itemSvgImage`，與「首次發現」一致
- **查看**：點擊背包中的道具 → 以**與首次發現相同的方式**呈現（浮動提示＋描述），不受場景限制，使用全域 `items[itemId]`
- **使用**：在特定場景使用道具觸發事件

---

## 【10】事件系統

### 事件觸發流程
1. 玩家點擊 HOTSPOT
2. 系統查找 `hotspotEventMap` 對應的事件 ID
3. 檢查事件 `requirements`（道具、flag、互動記錄等）
4. 條件滿足 → 觸發事件 → 應用 `effects`
5. 若為 `oneTime: true`，標記為已互動

### 事件效果類型
- `addItem` - 添加道具
- `removeItem` - 移除道具
- `setFlag` - 設置 flag
- `showDialog` - 顯示對話
- `playAudio` - 播放音效
- `triggerEvent` - 觸發其他事件
- `changeScene` - 切換場景

### 特殊事件：角色對話
- 事件 ID 匹配：`talk_to_character_*` 或 `character_*_talk`
- 自動使用 CharacterConversation 系統，從 `data/characterConversations.ts` 載入對話鏈

---

## 【11】分數與選擇系統

### 分數追蹤
- **總分數**：`state.score`（累加所有選擇的權重）
- **維度權重**：`state.weights`（如 acceptance, resistance, hesitation）
- **選擇記錄**：`state.choices`（時間戳與權重）
- **洞察三維度**：`state.insights`（procedure_insight, human_insight, evidence_insight）
- **偏好變量**：`state.preferences`（見 types/game.ts PreferencesState）

### 選擇權重
- 正權重：接受、承擔等；負權重：拒絕、離開等；零權重：中性

### 顯示組件
- `ScoreDisplay`：總分、各維度權重、選擇次數，整合在右上角選單

---

## 【12】音效系統（AudioManager）

- **檔案**：`lib/audioManager.ts`
- **音效類型**：環境音（Ambient）、廣播（Broadcast）、恐怖（Horror）、SFX
- **管理功能**：自動播放/停止環境音、音效音量控制、背景音樂管理
- **路徑**：`/audio/ambient/`、`/audio/broadcast/`、`/audio/horror/`、`/audio/sfx/`

---

## 【13】UI 系統

### 主要 UI 組件
- **選單系統**：右上角統一選單（退出、音效、背包、開發者模式、分數顯示）
- **背包**：浮動覆蓋層，不壓縮主畫面（Inventory、ItemIcon）
- **對話框**：固定尺寸 360×360px，支援打字機效果、標題、角色立繪（DialogBox）
- **角色對話框**：固定卡片寬高（CharacterConversationCard），右上角繼續＋X，下方固定高度按鈕區（選擇題）
- **場景 NPC 頭像**：NpcRightStrip，顯示當前場景 NPC，頭像尺寸約 +15% 適配
- **開發者面板**：`?dev=1` 啟用（DeveloperPanel）
- **章節介紹**：ChapterIntro — 分層顯示（Layer1 200ms、Layer2 800ms、Layer3 1600ms、按鈕 2600ms）；CTA 按鈕文字：無導讀影片或影片已播完為「開始調查」，有導讀影片且未播放為「觀看影片」；支援背景圖/影片/輪播、導讀影片、預載該章第一景背景圖

### 響應式設計
- `clamp()` 實現響應式尺寸；HOTSPOT 使用 `clamp(40px, 4vw, 64px)`；手機與桌面統一考量

---

## 【14】視覺效果系統

- **場景效果**：環境光調整、場景切換淡入淡出（無閃爍）
- **動畫**：Framer Motion 用於對話切換、組件進出；角色對話 opacity 動畫（無位置跳動）；打字機效果

---

## 【15】章節與場景系統

- **章節結構**：ch1～ch6（見 `data/chapters.ts`），每章含若干場景（scenes 陣列）；敘事上可有 ROOM/SPACE 概念。
- **章節介紹**：標題、副標題、描述、心理文字、背景圖/影片、導讀影片（`/play/[chapterId]/intro`），分層顯示後顯示「開始調查」或「觀看影片」按鈕。
- **場景切換**：左右箭頭、場景選擇器、自動保存

---

## 【16】開發與調試

- **開發者模式**：`?dev=1` → DeveloperPanel（狀態、快速跳轉、場景選擇器、Flag 管理）
- **Debug 模式**：`?debug=1` → 顯示所有 HOTSPOT 邊框與 id

---

## 【17】資料結構

### 主要資料檔案
- **data/gameDataCh1.ts**～**gameDataCh6.ts**：各章場景、道具、事件、謎題、NPC、npcDialogs；由 `getChapterData(chapterId)` 動態 import 載入（ch1～ch6）。
- **data/getChapterData.ts**：依 `chapterId` 動態 `import('./gameDataChX')` 回傳 `{ scenes, items, npcDialogs }`。
- **data/chapters.ts**：輕量章節定義（`chapters`、`prologueSlides`），供 intro / prologue / play 共用。
- **data/characterConversations.ts**：角色對話鏈（ConversationTurn[]、finalChoices）
- **data/reasoningByChapter.ts**：章節推理相關
- **data/flowConfig.ts**：流程設定（含 getChapterIntroContinuePath 等）
- **data/flagTestConfig.ts**、**ch1PasswordPresets.ts**、**ch1Monologue.ts**、**ch1ReportConfig.ts** 等：章節或流程用設定
- **types/game.ts**：所有 TypeScript 型別（GameState, Scene, Event, Dialog, DialogChoice, ConversationTurn, CharacterConversation, Item, Puzzle, Npc, InsightsState, PreferencesState 等）

### 狀態持久化
- **localStorage**：自動保存 GameState；頁面重新載入時恢復

---

## 【18】gameData 分章與按需載入（新增章節依據）

本專案採「輕量 chapters + 按章動態載入」架構，首屏與 intro 不載入整份 gameData；遊玩頁依 `chapterId` 動態載入該章資料並與 engine 合併。**新增章節時請依下列結構與流程操作。**

### 18.1 輕量章節定義 `data/chapters.ts`

- 匯出：`prologueSlides`、`chapters`（章節列表與 meta）。
- **新增章節時**：在 `chapters` 中新增一筆，例如：
  - `ch2: { id: 'ch2', name: '...', description: '...', scenes: ['scene_ch2_...', ...], intro: { title, subtitle, description, moodText, ambientAudio, introVideo, ... } }`
- 此檔僅含章節定義與序章文案，供 intro、prologue、play 共用；**不要**在此放 scenes/items/npcDialogs 細部內容。

### 18.2 分章資料檔 `data/getChapterData.ts` 與來源

- **取得章節資料**：`getChapterData(chapterId): Promise<ChapterData | null>` 由 `data/getChapterData.ts` 匯出。
- 內部依 `chapterId`（ch1～ch6）分別 `import('./gameDataCh1')` … `import('./gameDataCh6')`，回傳該章的 `scenes`、`items`、`npcDialogs`。
- **新增章節時**：新增對應 `data/gameDataChX.ts`，並在 `getChapterData` 的 if 分支中加入該章。

### 18.3 GameEngine 依賴注入與合併

- **檔案**：`lib/gameEngine.ts`。
- Engine **不**直接 import 完整 gameData；內部維護 `scenes`、`items`、`npcDialogs`（私有），並有：
  - `loadChapterData(data: { scenes, items, npcDialogs })`：以 `Object.assign` 與現有資料合併，不覆蓋已有 key。
  - `getScenes()` / `getItems()`：供 play 頁與需合併後資料的組件使用。
- **新增章節**：無須改 engine 邏輯，只要 `getChapterData(chX)` 回傳正確結構即可。

### 18.4 Play 頁載入流程 `app/play/[chapterId]/[sceneId]/page.tsx`

- **chapters**：從 `@/data/chapters` import。
- **scenes / items**：從 `engine.getScenes()` / `engine.getItems()` 取得（須在 `chapterDataReady` 後才有資料）。
- **載入時機**：`useEffect([chapterId])` 內呼叫 `getChapterData(chapterId)` → 取得後 `engine.loadChapterData(data)` → `setChapterDataReady(true)`。
- **首次渲染**：未載入前可顯示「載入場景…」，避免 `getCurrentScene()` 或 `scenes[sceneId]` 為空導致錯誤或閃「場景不存在」。
- **場景切換（箭頭）**：上一景／下一景依 `chapters[chapterId].scenes` 與當前 `sceneId` 計算（`getAdjacentScenes`），不依賴尚未載入的 scenes；箭頭點擊時先顯示大字特效、延遲後再切換 engine 與 URL，大字以 Portal 掛到 body 避免 early return 影響。

### 18.5 依 props 取得資料的組件（不直接 import gameData）

- **Inventory**、**DeveloperPanel**、**PickThreePuzzle**、**PairMatchingPuzzle** 等：由上層（play 頁）傳入 `items`、必要時 `scenes` / `chapters`（來自 engine 合併後或 play 頁 state），**不要**直接從 `@/data/gameDataChX` import。
- **新增章節**：只要 play 頁依 `getChapterData` 載入並合併，這些組件會自動拿到新章資料，無須改組件 import。

### 18.6 Intro / Prologue 頁

- **intro**：`import { chapters } from '@/data/chapters'`。
- **prologue**：`import { prologueSlides } from '@/data/chapters'`（或從 gameData 再匯出 chapters 的入口亦可）。

### 18.7 新增章節檢查清單

1. 在 `data/chapters.ts` 的 `chapters` 中新增該章（id、name、scenes 陣列、intro 等）。
2. 新增 `data/gameDataChX.ts`，export 該章 `scenes`、`items`、`npcDialogs`；並在 `getChapterData` 中對應到該章。
3. 確認 play 頁在 `chapterId` 為新章時能載入並顯示（載入中 → 場景列表與箭頭、背包、開發者面板皆正常）。
4. 依 props 取得資料的組件由上層傳入，無須改組件 import。
5. 跑一次 `npm run build` 與手動測試：進入新章 intro → 場景切換、背包、謎題、NPC 對話（若有）行為正確。

### 18.8 死碼與關閉功能

- 不保留 `ENABLE_SCENE_PUZZLES` 或 `false && showChapterPuzzle` 等死碼；若章節謎題日後啟用，改由流程／選單控制單獨載入，或從版控還原對應區塊。

---

## 【19】檔案命名規範

### 場景背景圖
- `bg_ch{章}_sc{場}_v{版}.webp`

### 道具圖
- `item_{id}_v{版}.webp`（優先）或 `.png`

### 角色立繪／頭像
- **WEBP（優先）**：`public/images/characters/{characterId}_{1|2|3}.webp`  
  例：`npc_lin_ruitang_1.webp`、`npc_ashun_2.webp`  
  由 `lib/characterPortrait.ts` 的 `getNpcPortraitUrl(characterId, expression)` 取得路徑
- **SVG（備援）**：`/svg/characters/{characterId}.svg`

### 音效
- 環境音：`/audio/ambient/{name}.mp3`
- 廣播：`/audio/broadcast/{name}.mp3`
- 恐怖：`/audio/horror/{name}.mp3`
- 音效：`/audio/sfx/{name}.mp3`

### 章節影片
- 導讀：`/videos/intro_ch{章}_animation_v{版}.mp4` 或自訂檔名（如 `木木.mp4`）

---

## 【20】重要設計原則

1. **型別安全**：所有遊戲內容透過 TypeScript 型別定義（types/game.ts），避免結構錯誤
2. **條件驅動**：事件與謎題基於 Requirement 條件系統，邏輯清晰
3. **狀態管理**：遊戲狀態由 GameEngine 統一管理，localStorage 持久化
4. **組件化**：UI 組件獨立（components/），邏輯與工具在 lib/，資料在 data/
5. **響應式優先**：UI 與 Hotspot 考量手機與桌面
6. **無跳過機制**：角色對話須完整觀看，繼續在右上角、選擇題在下方固定區
7. **視覺穩定**：淡入淡出動畫、固定卡片高度，避免「繼續」出現時跳動
8. **編譯相容**：CharacterConversation 主檔不以 JSX 回傳，改以 React.createElement(CharacterConversationCard) 避免部分環境下「Expected jsx identifier」錯誤
9. **一般對話立繪穩定**：同角色連續一般對話時不先清空 currentDialog，直接換下一則並用 effectiveDialog = currentDialog ?? dialogQueue[0] 驅動立繪，避免立繪先消失再出現
